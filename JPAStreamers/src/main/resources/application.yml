server:
  port: 2238
spring:
  application:
    name: JPAStreamers
  datasource:
    url: jdbc:mysql://localhost:3306/streamer
    username: root
    password: pass@word1
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true